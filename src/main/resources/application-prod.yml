## spring security 对外暴露接口设置
security:
  oauth2:
    client:
      ignore-urls:
        - /webjars/**
        - /token/**
        - /error
        - /code/**

# =========================
# 🧠 缓存 Redis 配置
# =========================
spring:
  cache:
    type: redis
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: wakeyoga123
      database: 0

  # =========================
  # 🗄️ 数据库配置（Druid）
  # =========================
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/kyd?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowMultiQueries=true&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true
      username: root
      password: 7f61cf0e00ccce1a

# =========================
# 🚀 RocketMQ 配置
# =========================
rocketmq:
  name-server: 14.103.193.249:9876
  producer:
    group: my-producer-group
    send-message-timeout: 10000
    retry-times-when-send-failed: 3
    retry-next-server: true

# =========================
# 📦 中科院专利接口配置
# =========================
yt:
  base-url: http://14.103.193.249:9898/admin/cnipr
  sf1-v1-url: ${yt.base-url}/page
  pi11-url:  ${yt.base-url}/pdfUrl
  pi12-url:  ${yt.base-url}/absUrl
  pi16-url:  ${yt.base-url}/imgUrl

model:
  huoshan:
    modelApiKey: 0ef4713a-70c1-4d45-9226-f6dade8d9026

cas:
  host: "https://e.kmmu.edu.cn/lyuapServer/"
  service: "https://kjcgzs.kmmu.edu.cn"

# =========================
# 🐷 PigX 配置
# =========================
pigx:
  mybatis:
    show-sql: false

# 文件上传配置
file:
  bucketName: prod # 上文创建的桶名称
  local:
    enable: true
    base-path: /data/kyd